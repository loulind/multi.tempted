format_multitempted <- function(featuretables, timepoints, subjID) {
  # 1. Checks validitiy of data
  # 2. Adjustment based on requested transformation
  # 3. Construct named list of lists of matrices (names = modality)
}
